[project]
name = "capstoneprojectdatasense"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "azure-identity",
    "langchain-core",
    "langchain-openai",
    "langgraph",
    "psycopg[binary]",
]
